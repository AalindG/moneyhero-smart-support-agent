# MoneyHero Backend — Claude Orchestration Rules

## Project Overview

Node.js + Express backend for MoneyHero Smart Support Agent.
LangChain.js RAG pipeline using HNSWLib + Ollama (llama3.2).
SQLite for persistence. SSE streaming on /api/chat.
Runs in Docker via docker-compose.yml.

## Agent Roster

### RAG Engineer

LangChain.js and RAG pipeline specialist. Builds document ingestion and conversational AI.
Owns vector store creation, intent classification, and agent logic.

### Principal Backend Developer

API and database layer ownership. Implements Express routes and SQLite operations.
Integrates RAG agent as a black box. Makes architectural decisions for API layer.

### Senior QA Engineer

Integration testing, API contract validation, error handling verification.
Writes test suites, validates Docker setup, ensures production readiness.

## Agent Routing Rules

### Parallel Dispatch

Use when tasks touch different files with no dependencies:

- rag-engineer (agent.js/ingest.js) + principal-backend-dev (routes/db.js/index.js)
- Scaffolder can run alongside docs generation

### Sequential Dispatch

Standard workflow:

1. **scaffolder** → sets up project structure
2. **rag-engineer** + **principal-backend-dev** → implement in parallel
3. **senior-qa-engineer** → validates, tests, integrates

**Critical dependencies:**

- Scaffold before implementation
- Docs must exist before ingest.js runs
- Both implementations complete before QA testing
- RAG agent exports chat() before routes can use it

### Background Dispatch

Use background processes for:

- npm install
- npm run ingest (slow — 30-60 seconds)
- docker compose up
- Any read-only exploration

## Agent Responsibilities

| Agent                 | Owns                                          | Never Touches          |
| --------------------- | --------------------------------------------- | ---------------------- |
| scaffolder            | package.json, .env.example, .gitignore,       | Any src/ logic         |
|                       | Dockerfile, docker-compose.yml, scripts/      |                        |
| rag-engineer          | src/agent.js, src/ingest.js                   | src/routes/, src/db.js |
|                       | RAG pipeline, vector store, intent classifier | src/index.js           |
| principal-backend-dev | src/db.js, src/routes/chat.js, src/index.js   | src/agent.js           |
|                       | API routes, database, Express setup           | src/ingest.js          |
| senior-qa-engineer    | tests/\*, README.md, integration validation,  | Core implementation    |
|                       | SSE streaming enhancement, error testing      | logic in src/          |

## Critical Constraints

- NEVER hardcode URLs or model names — always use process.env
- NEVER use @langchain/anthropic — LLM is Ollama only
- NEVER have two agents edit the same file in parallel
- Principal backend dev owns API/routes/db, treats agent.chat() as black box
- RAG engineer owns agent.js/ingest.js, never touches Express or SQLite
- Senior QA engineer validates but doesn't rewrite core logic
- vectorstore/ is generated by ingest.js — never manually created
- data/ folder holds SQLite DB — must be volume-mounted in Docker
- All code must use ES module syntax (import/export, not require)

## Environment Variables

PORT=3001
OLLAMA_BASE_URL=http://localhost:11434 (local)
=http://ollama:11434 (Docker — set in docker-compose.yml)
OLLAMA_MODEL=llama3.2:1b
OLLAMA_EMBED_MODEL=nomic-embed-text

## Docker Setup

- Services: backend (port 3001) + ollama (port 11434)
- Volumes: vectorstore/, data/, docs/ mounted from host
- OLLAMA_BASE_URL is automatically http://ollama:11434 inside Docker
- First-time setup: run ./scripts/setup.sh
- Daily use: docker compose up -d

## API Endpoints

All routes must match API_CONTRACT.md exactly.

POST /api/chat → SSE stream: data:{"token":"..."} then data:[DONE]
POST /api/escalate → logs escalation, returns { success, ticketId }
GET /api/history/:sessionId → returns { messages: [...] }
POST /api/session → returns { sessionId }

## Intent Classification

- answer → run RAG chain, return retrieved response
- escalate → return handoff message, log to escalations table
- off_topic → politely redirect to financial product questions

## Error Handling

- 200: success with data
- 400: { error: "message" } for client errors
- 500: { error: "message" } for server errors
- Never crash on unhandled errors

## Build Sequence Checklist

1. [ ] Scaffold folders, package.json, Docker files
2. [ ] Create 11 sample docs in /docs
3. [ ] Generate API_CONTRACT.md
4. [ ] Build src/ingest.js + src/db.js in parallel
5. [ ] Build src/agent.js + src/routes/chat.js + src/index.js in parallel
6. [ ] Run npm run ingest — verify /vectorstore created
7. [ ] Integration pass by senior-qa-engineer
8. [ ] docker compose up — verify backend on port 3001
